#include "lists.h"
/**
 * check_circle -checks if a singly linked list has a cycle in it.
 * @list - singly linked list
 * Return: 0 if there is no cycle, 1 if there is a cycle
 */

 int check_circle (listint * list)
 {

 }
